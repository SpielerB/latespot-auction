NFT Contract
  setMetadataContract
  minting
  staking

Metadata Contract
  setNftContract
  seed -> generate metadata
  tokenMetadata(uint256 tokenId) ->
  randomMeta(seed, tokenId) -> stakeLevel:
  stakeLevel(uint256 tokenId, uint256 level) onlyNFTContract;
  
IPFS:
  squirrels/00001-0.png -> No Pass
  squirrels/00001-1.png -> Bronze Pass
  squirrels/00001-2.png -> Silver Pass
  squirrels/00001-3.png -> Gold Pass
  
Single Stake, once redeemed = locked