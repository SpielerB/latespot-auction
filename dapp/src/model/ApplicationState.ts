enum ApplicationState {
    DISCONNECTED,
    PRE_AUCTION,
    PRIVATE_AUCTION,
    PRE_PUBLIC_AUCTION,
    PUBLIC_AUCTION,
    PRE_MINT,
    PRE_REVEAL,
    STAKING
}

export default ApplicationState;